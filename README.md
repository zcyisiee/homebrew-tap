# homebrew-tap

Homebrew tap for [Tearoff](https://github.com/zcyisiee/Tearoff).

```bash
brew tap zcyisiee/tap
brew install --cask tearoff
```

`Casks/tearoff.rb` is updated automatically by the Tearoff release workflow.
